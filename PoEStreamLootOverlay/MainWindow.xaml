<Window x:Class="PoEStreamLootOverlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PoEStreamLootOverlay"
        mc:Ignorable="d"
        Title="PoEStreamLootOverlay" Height="450" Width="483.333">
    <Grid Background="#FF212121">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Content="Reset" HorizontalAlignment="Left" Margin="296.381,394,0,0" VerticalAlignment="Top" Width="75" Grid.Column="1" Click="Button_Click_1"/>
        <Button Content="Settings" HorizontalAlignment="Left" Margin="296.381,369,0,0" VerticalAlignment="Top" Width="75" Grid.Column="1" Click="Button_Click"/>
        <ItemsControl
            x:Name="currencyView" Grid.RowSpan="1"
            >
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:CurrencyControl></local:CurrencyControl>

                </DataTemplate>
            </ItemsControl.ItemTemplate>
        
        </ItemsControl>
    </Grid>
</Window>
